import { PaymentMethodsManager } from "@/components/settings/payment-methods-manager";

export const metadata = {
  title: "Metode Pembayaran - Prima Motor POS",
};

export default function PaymentMethodsRoute() {
  return <PaymentMethodsManager />;
}
